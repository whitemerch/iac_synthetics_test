replicaCount: 1

image:
  repository: nginx
  tag: latest
  pullPolicy: IfNotPresent

securityContext:
  privileged: true   # ❌ Misconfiguration: should not be privileged

service:
  type: LoadBalancer  # ❌ Exposes externally by default
  port: 80
